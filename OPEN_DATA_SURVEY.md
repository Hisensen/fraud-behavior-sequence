# 开源反欺诈数据集可行性普查 + MEM 实测结果

> 日期：2026-07-03
> 问题：网上那些开源反欺诈数据，哪些真的可用？效果如何？
> 方法：先按"能否按账户聚合出带时间戳的事件序列 + 有无可靠标注 + 能否下载"
> 三条标准初筛，可行的全部接入 MEM 流水线实测（统一协议：无监督训练、
> 账户/窗口级切分防泄漏、监督 LR oracle 做信号上限参照）。

---

## 一、普查总表（10 个主流数据集）

### 结构上不可行（4 个，不用浪费时间）

| 数据集 | 死因 |
|---|---|
| ULB creditcard（28万笔） | 特征是 PCA 匿名的 V1~V28，**无账户 ID**，聚不出序列 |
| IEEE-CIS / Vesta（59万笔） | 无显式用户 ID；竞赛数据需登录+接受条款 |
| BAF（NeurIPS'22） | 一行=一次开户申请，横截面表格，无事件流 |
| Elliptic / Elliptic++（BTC） | 特征按 49 个时间步聚合，无逐笔序列（图方法专用） |

### 实测判定 + MEM 结果（6 个）

| 数据集 | 真实性 | 可行性判定 | MEM 无监督 | 监督 oracle | 差距 |
|---|---|---|---|---|---|
| **Ethereum 钓鱼**（XBlock） | ✅ 真实 | ✅ 推荐 #1 | **0.904** | 0.934 | 0.03 |
| **Sparkov**（信用卡） | 拟真 | ✅ 推荐 #2 | 0.848（字段遮罩） | 0.880 | 0.03 |
| **TabFormer**（信用卡, IBM） | 拟真 | ✅ 推荐 #3 | 0.687 / 定位 0.833 | 0.760 | 0.07 |
| **BankSim**（银行支付） | 拟真 | ✅ 可用 | 0.736 / **定位 0.853** | 0.891 | 0.16* |
| **IBM AML**（转账/洗钱） | 拟真 | ⚠️ 序列弱、需图 | 0.656 | 0.695 | 0.04 |
| **PaySim**（手机转账） | 拟真 | ❌ 无信号 | 0.561 | **0.610** | 0.05 |

\* BankSim 差距偏大的原因：训练窗口污染率高达 19%（欺诈密度远超其他数据），
超出了污染鲁棒性实验验证过的 10% 上限——是我们已知边界的实证，不是意外。

---

## 二、两个数据集的"验尸报告"

### PaySim：名声大，但对序列方法基本无用
- **发起方账户实测**：635 万账户，中位出现 **1 次**、最多 3 次——业界对它
  "账户连不成序列"的质疑属实；
- 改用收款账户视角（2416 个欺诈收款账户，≥10 笔）后序列可以建，但
  **oracle 用全部标签也只有 0.610**——它的欺诈是随机注入的
  TRANSFER→CASH_OUT，没有行为纹理，序列里本来就没有信号；
- 判定：做序列方法评估**不要用 PaySim**；它只适合练习表格特征工程。

### IBM AML：信号在图里，不在单账户序列里
- 508 万笔转账、4388 个洗钱账户，序列完全可建；
- 但 oracle 只有 0.695、MEM 0.656——洗钱的本质模式是**跨账户结构**
  （分散转入-集中转出、环形转账、多层过账），单账户序列只能看到局部片段；
- 这份数据反过来实证了"图盲区"的存在：要吃下 AML 类场景，必须加图特征。

---

## 三、贯穿六个数据集的核心规律

**MEM 无监督分数与"用全部标签的监督 oracle"差距稳定在 0.03~0.07 AUC**
（BankSim 因超限污染例外）。这意味着：

1. 方法本身是可靠的——数据里有多少序列信号，它不用标签就能抽出大部分；
2. 各数据集的效果差异（0.56~0.90）反映的是**数据本身的信号强度**，
   不是方法的不稳定；
3. 拿到新数据（如北部湾业务数据）时，先跑 oracle 就能预测 MEM 大概落在哪。

配方选择规律（已在多数据集复验）：
- 资金流（转账类）→ 整事件遮罩 + mean 池化（以太坊 0.904、AML 最优组合也含全字段）；
- 消费流（卡交易类）→ 字段级遮罩 + top-k（Sparkov 0.557→0.848）；
- 操作流（App 日志类）→ 整事件遮罩 + top-k（temporal 0.959）；
- 欺诈密度 >10% 的数据 → 无监督前提失效，需先粗筛再训练。

---

## 四、文件索引（本轮新增）

| 文件 | 数据集 | 结果 |
|---|---|---|
| `paysim_experiment.py` | PaySim 收款账户 | 0.561（数据无信号） |
| `banksim_experiment.py` | BankSim 字段遮罩 | 0.736 / 定位 0.853 |
| `aml_experiment.py` | IBM AML HI-Small | 0.656（信号在图） |
| 数据缓存 | `~/.cache/kagglehub/datasets/` | PaySim 470MB / BankSim 77MB / AML 447MB |
