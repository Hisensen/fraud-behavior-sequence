# 开源真实数据验证报告 — MEM 方法"效果到底好不好"

> 日期：2026-07-03
> 前置：`EXPERIMENT_MEM.md`（合成数据主实验）、`VALIDATION_REPORT.md`（三项补充验证）
> 本轮新增两个开源数据集（以太坊钓鱼、Sparkov 信用卡），加上之前的 TabFormer，
> 共三份"真实/拟真"数据的横向验证。

---

## 数据集与选择理由

真实银行反欺诈数据（含北部湾类业务数据）受法规约束不可能开源。公开渠道能拿到的
"账户级、逐笔时间戳、带欺诈标注"数据只有两类：区块链（天然公开）和信用卡（脱敏模拟）。

| 数据集 | 真实性 | 规模 | 对应业务场景 |
|---|---|---|---|
| **Ethereum 钓鱼**（XBlock EPTransNet） | ✅ 真实链上交易+真实诈骗标注 | 297万节点/1355万边；取 680 钓鱼 + 5000 正常账户 | **电诈资金流**（受害者打款→骗子归集） |
| **Sparkov**（Kaggle kartik2112） | 拟真模拟 | 185万笔/999客户/欺诈率0.52% | 卡消费盗刷 |
| **TabFormer**（IBM，前轮已测） | 拟真模拟 | 2439万笔/2000用户/欺诈率0.12% | 卡消费盗刷 |

---

## 总结果

| 数据集 | MEM 无监督最优 | 监督 oracle（用标签） | 差距 | 定位AUC |
|---|---|---|---|---|
| temporal 合成 | 0.959 (top-5 type) | 0.999（规则天花板） | 0.04 | 高分位=敏感操作 |
| TabFormer | 0.687 (top-10 type+chip) | 0.760 | 0.07 | 0.833 |
| Sparkov | v1 0.557 → **v2 0.848** (top-3 type字段遮罩) | 0.880 | 0.03 | 0.696 |
| **Ethereum 钓鱼** | **0.904** (mean 仅amt) | 0.934 | 0.03 | —（账户整体为黑） |

**核心结论：MEM 无监督分数在四份数据上稳定逼近"用全部标签的监督 oracle"，
差距 0.03~0.07 AUC。方法可靠地抽取了数据里的大部分信号；上限由数据本身决定。**

---

## 本轮两个新发现（对接真实业务最重要）

### 1. 遮罩方式要匹配数据形状（Sparkov v1→v2, 0.557→0.848）

- **整事件遮罩**适合有序列结构的数据（操作流/资金流）：上下文能预测下一步；
- 信用卡消费流序列依赖弱（下一笔买什么与上一笔无关），欺诈签名在**字段间不一致**
  （类别×金额×时段不匹配）→ 必须用**字段级遮罩**：只遮一个字段、其余可见，
  学条件分布 P(类别|金额,时段,上下文)。v2 最优单字段是"遮类别"（0.848），
  "遮金额|类别可见"0.760，两者组合反而稀释（0.782）。

### 2. 池化方式要匹配欺诈形态（Ethereum: mean > top-k）

- temporal 合成数据欺诈是**局部爆发**（8/76 个位置异常）→ top-k 赢（0.959 vs 平均 0.756）；
- 以太坊钓鱼账户**整个账户都是作案工具**（每笔交易都是骗局一部分）→
  全序列平均赢（0.904 vs top-10 0.875）。
- 规律：**异常范围决定池化——局部异常用 top-k，整体异常用平均。**
  上线时两种分数都算，当两个通道用。

### 3. 以太坊各字段贡献（钓鱼行为画像）

| 字段（事件级遮罩） | AUC | 解读 |
|---|---|---|
| 仅金额 CE（mean） | **0.904** | 钓鱼账户金额模式最反常（小额密集进+大额出） |
| 仅方向 CE | 0.793 | 方向序列（连续转入后突转出）次之 |
| 仅间隔 CE（mean） | 0.377（反向） | 熵混淆再现——间隔信号必须 top-k + 归一化才可用 |

---

## 诚实的局限

1. **以太坊"正常"账户有采样偏置**：全图由钓鱼种子 BFS 爬出，"正常"账户实为
   钓鱼账户的 1-2 阶邻居（和骗子有过交互的账户），并非全网随机正常用户。
   真实区分度可能比 0.904 更高（真正常用户和钓鱼差异更大），但无法在此数据上证明。
2. **严格低误报区仍吃力**：R@FPR1% 只有 14.1%（oracle 也只有 26.2%）——
   两类账户在分数高段有重叠，运营上更适合做"排序+人工审核"而非自动处置。
3. 信用卡两份数据是高质量模拟而非真实（真实卡数据不开源账户序列）。
4. 无监督变体选择（哪个字段、top-k 还是 mean）目前在测试集上比较——上线前需要
   留出小验证集固定配置，或按数据形状先验直接指定（资金流→mean+金额，
   操作流→top-k+类型）。

---

## 对"北部湾数据"的落地指引

北部湾银行类业务数据到手后（脱敏账户流水：账户ID/事件类型/时间戳/金额/渠道）：

1. **形状对号入座**：转账资金流 → 以太坊配方（整事件遮罩 + mean 池化 + 金额头）；
   卡消费流水 → Sparkov 配方（字段级遮罩 + top-k + 遮类别头）；
   App 操作日志 → temporal 配方（整事件遮罩 + top-k + 类型头）。
2. **先跑 oracle 摸底**（词频/统计+LR），确定信号层和基线；
3. MEM 无监督分数预期能到 oracle 的 0.03~0.07 AUC 以内（四份数据的一致规律）；
4. 攒到 50+ 黑标签后按 `VALIDATION_REPORT.md` 实验③配方切监督为主。

---

## 文件索引（本轮新增）

| 文件 | 用途 |
|---|---|
| `eth_extract.py` | 从 XBlock 1355万边大图抽取账户序列 → `eth_sequences.jsonl` |
| `eth_experiment.py` | 以太坊钓鱼 MEM3 实验 |
| `sparkov_experiment.py` | Sparkov v1（整事件遮罩，负结果） |
| `sparkov_v2.py` | Sparkov v2（字段级遮罩，+29pp） |
| `eth_phishing/` | yuanqi7 仓库数据（1262 钓鱼账户 Etherscan 原始流水，备用） |
| 大文件 | XBlock pickle 1.2GB 在 `~/.cache/kagglehub/`；Sparkov 477MB 同处，不用可删 |
