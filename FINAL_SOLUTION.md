# 定稿方案 — 分层反欺诈系统（Capstone）

> 日期：2026-07-06
> 本文回答两个问题：目前的方法是不是"最好"？以及把全部实验结论组装成
> 一个完整的、可落地的最终方案。

---

## 一、"是不是最好的方法"——分三层诚实回答

**1. 在"无标签、单账户序列、轻量算力"这个约束框内：接近最优，有证据。**
六个可提升数据集中五个已达到或超过各自监督 oracle 的天花板
（Sparkov 0.886>0.880、AML 0.712>0.695、TabFormer 0.752≈0.760、
Ethereum 0.928 距 0.934 仅 0.006、rich 0.984）。天花板是数据本身的
信号总量决定的——**在这个框内继续换方法，已无可赚的空间**。

**2. 跳出这个框，有三个已知的"更好"，但各有代价：**
- **监督学习**（50+ 标签后）：实验③证明从零监督 0.996 碾压一切无监督——
  标签攒够后主判别就该换它，MEM 退居特征与新型欺诈兜底；
- **图方法**（团伙场景）：共享设备/资金链信号是序列方案的结构性盲区，
  AML 类场景的真正上限在图里；
- **更大的预训练模型**：行为序列的"BERT 时刻"——用海量跨机构序列预训练
  大模型再微调。方向成立但需要百万级序列与 GPU 集群，不在当前资源框内。

**3. 所以正确的问法不是"哪个方法最好"，而是"怎么组装最好"。**
反欺诈没有银弹——规则抓已知、无监督抓未知、监督抓确认过的、图抓团伙，
四者互补而非互替。下面就是组装方案。

---

## 二、定稿架构：五层系统

```
                 ┌─────────────────────────────────────┐
   原始数据 ──→  │ L0 数据底座: 统一Schema + 特征字典     │
                 └──────────────┬──────────────────────┘
        ┌───────────┬───────────┼───────────────┐
        ▼           ▼           ▼               ▼
   ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌────────────┐
   │L1 规则层 │ │L2 序列层 │ │L3 聚合层  │ │L4 监督层    │
   │已知模式  │ │MEM/AR   │ │比例/画像  │ │(标签攒够后) │
   └────┬────┘ └────┬────┘ └────┬─────┘ └─────┬──────┘
        └───────────┴─────┬─────┴──────────────┘
                          ▼
                 ┌─────────────────┐     ┌──────────────┐
                 │  分数融合 + 分层阈值 │ ←→ │ L5 图层(团伙)  │
                 └────────┬────────┘     │  路线图项      │
                          ▼              └──────────────┘
                 人工审核队列(带top-k证据) → 反馈标签 → 回流训练
```

### L0 数据底座（一切效果的上限在这里）

统一 Schema 编码（ENCODING_REPORT 定稿版）：
`[type, result, channel, ip_change, 全局金额桶, 个人金额桶, 间隔桶, 时刻]`，
缺失填 0 号桶。字段优先级（按实证收益）：

| 优先级 | 字段 | 实证 |
|---|---|---|
| ★★★ | 个人相对金额（÷个人滚动中位数） | Sparkov +3.8pp, 定位+19pp |
| ★★★ | result（成功/失败） | rich 消融 +22pp |
| ★★★ | 对手方新旧（首次/复现次数） | AML +5.6pp, Ethereum 融合+2.5pp |
| ★★ | channel / 设备 / ip 变化 | TabFormer chip +10pp |
| ★ | 全局金额桶、时刻 | 基线组成部分 |

### L1 规则层（第一道闸，零成本高解释）

profile 摸底中词频/统计 oracle 能单独打高分的信号，直接写成规则
（如"敏感操作前间隔≤30min 计数"在 temporal 上 AUC 0.999）。
规则抓已知模式最快最稳，也给模型分流：**规则能杀的不劳模型**。

### L2 无监督序列层（本项目主体，抓"行为语法异常"）

按数据形状三档配置（ARCH_REPORT 定稿）：

| 形状 | 目标 | 池化 | 架构 | 实证 |
|---|---|---|---|---|
| 操作流 | 自回归下一事件 | top-k | +时间偏置注意力 | rich 0.984 |
| 资金流 | 整事件遮罩 | mean | 标准 | Ethereum 0.904 |
| 消费流 | 字段级遮罩 | top-k | 标准 | Sparkov 0.886 |

消费流用**双通道**：字段遮罩管判黑 + 整事件遮罩管逐笔定位（TabFormer 结论）。
打分统一走：逐位置 CE → 按间隔桶 z-norm（省略即反向！）→ 池化。
训练用 unlabeled 当白样本（≤10% 污染免疫；>15% 需 L4 少量标签预过滤，
自清洗已证伪）。

### L3 聚合特征层（静态信号，只走融合不进模型）

机制规律（IMPROVEMENT_REPORT）：**比例/计数类静态特征塞进预测通道无效**
（模型学会即不惊讶），必须以裸特征参与**分数级秩融合**：
- 首次对手方占比（Ethereum 实证：融合 +2.5pp、低误报召回×3）
- 转入转出比、深夜操作占比、金额速度(velocity) 等
融合权重用少量标签或验证集定（默认 序列分0.6 / 聚合分0.4）。

### L4 监督层（标签成长后的主判别）

标签数量驱动的三段切换（VALIDATION_REPORT 实验③）：
- **0~10 个**：纯 L2+L3 融合分；
- **10~50 个**：标签只做两件轻量的事——融合权重学习 + 阈值校准
  （不要拿去微调模型，已证伪）；
- **50+ 个**：从零训监督小模型接管主判别（0.996 级），
  L2 异常分降级为其输入特征 + 新型欺诈兜底通道。

### L5 图层（路线图项，团伙场景的钥匙）

序列方案的结构性盲区。分两步走：
1. **便宜的先做**：图统计量当 L3 聚合特征（对手方度数、共享设备账户数）；
2. **需要时再上**：账户-设备-对手方图 + 社区发现，整窝端团伙。
触发条件：L2+L3 上线后漏掉的案件复盘显示团伙特征时启动。

### 运营闭环

- **阈值**：白样本分位数定（p99=误报1%），按用户活跃度分桶各定各的，每月重算；
- **输出**：风险分 + top-k 可疑事件位置（出证给审核员），只排序不自动处置；
- **反馈**：审核结论回流为标签 → 驱动 L4 演进 + L2 季度重训；
- **监控**：白样本分数分布漂移报警（分布移动=用户行为变了或模型过期）。

---

## 三、分阶段落地（对接真实数据）

| 阶段 | 条件 | 动作 | 预期 |
|---|---|---|---|
| P0 摸底 | 拿到数据 | pipeline ①~③：inspect/convert/profile | 半天知道信号在哪层、上限多少 |
| P1 冷启动 | 无标签 | L1 规则 + L2 按形状建模 + L3 融合，输出审核队列 | oracle−0.03~0.07 |
| P2 成长 | 攒到 10~50 标签 | 融合权重学习 + 阈值校准 | 低误报召回显著提升 |
| P3 成熟 | 50+ 标签 | L4 监督主判别上线，L2 转兜底 | 逼近 0.99 级 |
| P4 扩展 | 团伙漏案出现 | L5 图统计→图模型 | 覆盖序列盲区 |

---

## 四、边界声明（这个方案不承诺什么）

1. 数据无信号则全链路无效（PaySim 教训）——P0 摸底就是止损闸；
2. 单账户序列看不到团伙协同，L5 之前对"跑分水房"类案件覆盖有限；
3. 对抗漂移（骗子放慢节奏）未实测，靠多尺度间隔桶+定期重训缓解，需上线后观察；
4. 所有合成数据上的数值是机制证明，真实场景数值以 P0 摸底为准。
```
