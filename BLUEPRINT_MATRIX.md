# 实验矩阵报告 —— 15 种建模思想的实证判决

数据：`data_cluster.jsonl`（4 白人群原型×500 + 4 黑手法×60，双真值）。
训练只用 1600 白样本；检测 AUC 在测试集（400 白+240 黑）；
白 ARI = 2000 白聚 4 人群；黑 ARI = 240 黑聚 4 手法。
脚本：`blueprint_prep.py` + `exp*.py`，共享产物 `blueprint_art.npz`。

## 一页总表

| # | 思想 | 检测 AUC | 白 ARI | 黑 ARI | 判决 |
|---|---|---|---|---|---|
| 1 | Distribution（密度） | 0.92(嵌入)/0.99(统计iForest) | - | - | ✅ 成立，正常分布确为多模态（BIC 选 K=12） |
| 2 | Prototype（原型） | K=1: 0.52 → K=16: **0.92** | 0.97 | - | ✅ 成立，但**单中心彻底失败**，多原型是刚需 |
| 3 | Seq Embedding | - | **0.97** | ≈0 | ✅ 聚人群近乎完美；对手法**完全失明**（前次实验） |
| 4 | Contrastive（CoLES-lite） | 0.91(kNN) | **0.98**（全场最高） | 0.34~0.52 | ✅ 人群表征最优；比 MEM 嵌入多一点手法信号 |
| 5 | Grammar（语法） | 一元0.72 / 二元0.66 / MEM 0.78 | - | - | ⚠️ 本数据顺序信息弱（打乱后 NLL 仅+0.02）；顺序价值依赖数据形状（temporal 基准上曾证明强） |
| 6 | Motif | 罕见度 0.80 | 0.84 | 0.33 | ⚠️ 检测中游；聚类不如词频 baseline，不留 |
| 7 | Fingerprint（惊讶画像） | - | 0.27 | **0.587**(KMeans) / **92.5%**(kNN手法命中) | ✅ 手法归因成立（前次+本次） |
| 8 | Trajectory（DTW 日轨迹） | 0.58 | 0.00 | 0.29 | ❌ 不成立，全面垫底，放弃 |
| 9 | Retrieval（向量检索） | 第5近白距离 0.94(指纹)/0.98(统计)；**120案件库→邻居黑占比 AUC 0.984** | - | 手法命中 92.5% | ✅✅ **性价比之王** |
| 11 | World Model（MEM） | top5 0.78 / **mean 0.88** | - | - | ✅ 成立；再证池化定律（整段型手法→mean 优于 top-k） |
| 12 | VAE（潜因子） | 重构误差 **0.988**（单视角最高） | 0.71 | 0.47 | ⚠️ 检测强但≈PCA(0.97)；"行为DNA因子"不成立（8因子全糊在金额桶上，潜变量聚类反而不如原始特征） |
| 14 | Multi-view 融合 | MEM+对比kNN 0.94；全视角秩融合 **0.986** | - | - | ✅ 成立，多视角增益真实 |

未跑：#10 图（无对手方/设备字段，既定推迟）；#13 因果（无干预数据）；#15 基础模型（此前 finetune_fair 已证跨数据预训练迁移为负收益）。

## 意外发现（比总表更重要）

**1. 统计特征是本基准的隐形冠军。**
iForest/OCSVM/VAE 在 ~40 维手工统计特征上检测 0.986~0.988；
原始统计特征聚类白 0.89、**黑手法 0.91**（超过惊讶画像的 0.59）。
原因：本数据的 4 种手法都大幅改变聚合统计（凌晨占比、间隔分布、个金桶分布、事件量），
而 data_cluster 造数时只做了部分词频对齐。**这不是"MEM 不如 iForest"**——
此前 temporal 基准（严格词频对齐）上统计方法失效、MEM 0.96，两组实验合起来的正确结论是：

> 信号在哪一层，哪一层的方法就赢。聚合信号存在时简单统计方法就能吃掉大头；
> MEM 的价值在聚合信号被抹平后的条件结构残差。生产上两层都要，融合兜底。

**2. 单中心假设（DeepSVDD 式）在行为数据上不成立。**
K=1 原型 AUC 0.52（等于抛硬币），K=16 升到 0.92。
"正常"不是一个球，是多个岛——任何单中心方法直接出局。

**3. 检索是被低估的赢家。**
零训练：第 5 近白样本距离（统计空间）AUC 0.979。
加 120 个已结案案件：邻居黑占比 AUC 0.984、手法归因命中 92.5%（指纹空间）。
且指纹空间检索手法命中率(92.5%)远超嵌入空间(69.2%)——再次验证"手法住在偏移空间"。

**4. 池化定律第三次得到验证。**
本数据手法多为整段型（慢速抽干/养卡/赌博贯穿全月）→ mean(0.88) > top5(0.78)，
与"局部爆发用 top-k、整段异常用 mean"完全一致。

**5. 对比学习值得留但不急。**
白 ARI 0.98 全场最高，但只比免费的 MEM 嵌入(0.97)高一点；
其对手法的少量感知(0.3~0.5)不稳定（两次消融差 0.18）。定位：P1 的升级备胎（原 S5 路线不变）。

## 落地修订（对五层方案的增量）

1. **评分主链**加"统计特征异常通道"（iForest 或 kNN-统计距离），与 MEM 惊讶度秩融合
   —— 全视角融合 0.986 vs 单 MEM 0.78 的差距必须吃下。
2. **检索层升格**：从"远期 C4"提前到第一批落地——零训练版当天可上，
   案件库版只要 100+ 结案样本。
3. **手法归因双轨**：惊讶画像 KMeans/HDBSCAN（无案件时）+ 指纹空间 kNN 案件检索（有案件后）。
4. **放弃**：DTW 轨迹、Motif 聚类、VAE 潜因子解释（保留 VAE/iForest 重构误差作检测通道之一）。
5. 池化方式按手法形态自适应：报警时同时报 mean 分与 top-k 分（已有多头基础，零成本）。
