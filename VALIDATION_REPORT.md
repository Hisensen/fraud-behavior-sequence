# MEM 方案三项补充验证报告

> 日期：2026-07-03
> 前置：`EXPERIMENT_MEM.md`（temporal 合成数据上 MEM 无监督 AUC=0.959）
> 本报告回答三个问题：训练数据被污染还能用吗？真实数据上表现如何？
> 少量标签怎么用最划算？

---

## 实验①：污染鲁棒性 ✅ 通过

**设定**：往 400 白样本的训练集里混入黑样本（0/1%/5%/10% 四档），
所有档共用同一测试集（100 白 + 456 黑），打分方式 z-norm top-5 type CE。

| 污染档 | 训练集构成 | AUC | KS | R@FPR1% |
|---|---|---|---|---|
| 0% | 400白+0黑 | 0.9605 | 0.788 | 70.8% |
| 1% | 400白+4黑 | 0.9647 | 0.796 | 75.4% |
| 5% | 400白+21黑 | 0.9671 | 0.818 | 78.1% |
| 10% | 400白+44黑 | 0.9598 | 0.796 | 73.5% |

**结论**：10% 污染下 AUC 几乎不掉，轻度污染甚至略升（正则效应）。
机理：被污染序列里异常位置只占 ~10%（8/76），异常 (类型,间隔) 组合
在全部训练信号中占比 ~1%，模型来不及把它学成"正常"。
**真实场景直接拿 unlabeled 数据当白样本训练是可行的**——真实欺诈率
(0.1~1.7%) 远低于我们测过的 10%。

---

## 实验②：真实数据验证（IBM TabFormer）⚠️ 部分通过

**数据**：TabFormer 信用卡交易，2000 用户 / 2439 万笔 / 欺诈率 0.122%，
1343 个用户含欺诈。按 User 聚合、时间排序、切 64 笔不重叠窗口；
白窗口来自 657 个干净用户（按用户切 80/20 训练/测试防泄漏），
黑窗口 = 欺诈用户中含 ≥1 笔欺诈的窗口。测试 1500 白 + 2000 黑窗口。

**窗口级结果**（v1 无 chip 字段 → v2 加 chip 渠道字段）：

| 方法 | AUC | 备注 |
|---|---|---|
| MEM v1 z-norm top-3(仅type) | 0.585 | 缺渠道字段 |
| MEM v2 z-norm top-10(type+chip) | **0.687** | 无监督，0 标签 |
| 监督 oracle：MCC+chip+金额+LR | 0.760 | 用了全部标签(5折CV) |
| 监督 oracle：仅 online 占比 | 0.598 | 单特征 |

**定位级结果**（黑窗口内逐笔交易，z 分 vs 是否欺诈）：

| 版本 | 位置级 AUC |
|---|---|
| v1（仅type） | 0.710 |
| v2（type+chip） | **0.833** |

**结论与发现**：
1. **无监督 MEM(0.687) 已接近全标签监督 LR 天花板(0.760)**——这个
   数据集的窗口级欺诈信号本身就弱，不是方法失效；
2. **定位能力是最亮的结果**：不用任何标签，模型能在含欺诈的窗口内把
   欺诈交易排到前面（AUC 0.833），可直接做"可疑交易高亮"；
3. **gap（时间间隔）信号在真实数据上是负贡献**（type+chip 0.687 >
   type+gap+chip 0.606）——TabFormer 的欺诈没有"时序爆发"结构，
   合成数据的核心假设在这个数据集上不成立。字段选择要跟数据走；
4. 用户级 max 聚合 AUC=0.448 是采样伪影（白用户人均 20 个测试窗口、
   黑用户只保留了含欺诈窗口约 2 个，max 天然偏袒窗口多的一方），
   不代表方法失效，公平的用户级评估需每用户等量窗口，留待后续。

---

## 实验③：少量标签怎么用 —— 结论反直觉 ❗

**设定**：100 黑样本进标签池，测试集固定为 100 白 + 400 黑。
四种方法同测试集可比，N∈{10,50,100} × 3 种子。

| N黑标签 | 方法 | AUC (mean±std) | R@FPR1% |
|---|---|---|---|
| 0 | (a) 纯无监督 MEM 异常分 | **0.9607** | 71.2% |
| 10 | (b) 预训练+微调(公平40ep) | 0.783±0.090 | 32.7% |
| 10 | (c) 从零监督 | 0.921±0.058 | 53.6% |
| 10 | (d) 微调+无监督融合 | 0.925±0.068 | 60.2% |
| 50 | (b) 预训练+微调(公平40ep) | 0.964±0.025 | 75.2% |
| 50 | (c) 从零监督 | **0.996±0.003** | 94.5% |
| 100 | (c) 从零监督 | **0.999±0.002** | 99.8% |
| — | 线性探针(冻结编码器) N=50 | 0.649±0.017 | 4.7% |

**结论**：
1. **N≤10 时纯无监督最强**（0.961 > 0.921±0.058）——标签太少时监督
   训练方差巨大，异常分数更稳；
2. **N≥50 时从零监督碾压**（0.996），而且**预训练初始化反而拖后腿**
   （公平对照下 0.964 < 0.996，唯一变量是初始化）；
3. 线性探针只有 0.65：MEM 编码器 mean-pool 表征里**没有**线性可分的
   欺诈信号。MEM 的判别力不在表征里，而在"逐位置审问"的打分过程里；
4. **MEM 的正确定位是"异常分数生产者"，不是"预训练编码器"**。
   "预训练+微调"这个方向（原项目改进方向 3）在此设定下证伪。

**务实配方**：标签 <10 → 纯无监督异常分；标签 ≥50 → 从零监督为主、
异常分做补充特征/兜底新型欺诈（监督模型只认识见过的模式）。

---

## 总体判断

| 问题 | 答案 | 证据 |
|---|---|---|
| unlabeled 当白样本可行吗 | ✅ 可行 | 10% 污染 AUC 不掉 |
| 方法能迁移到真实数据吗 | ⚠️ 能，但信号强度取决于数据 | 0.687 vs 天花板 0.760；定位 0.833 |
| 该拿它当预训练底座吗 | ❌ 不该 | 公平对照+线性探针双重证伪 |
| 时序爆发假设普适吗 | ❌ 不普适 | TabFormer 的 gap 是负贡献 |

一句话：**MEM 异常检测作为"零标签冷启动 + 可疑交易定位"工具是站得住的；
有 50+ 标签后应切换/融合监督模型；不要把它当预训练编码器用。**

---

## 文件索引（新增）

| 文件 | 用途 |
|---|---|
| `contamination.py` | 实验① 污染鲁棒性 |
| `tabformer_experiment.py` / `tabformer_v2.py` | 实验② v1/v2 |
| `finetune.py` / `finetune_fair.py` | 实验③ 及公平对照 |
| `tabformer_windows.pkl` | TabFormer 窗口缓存（复跑免重新解析 CSV） |
| `tabformer_mem5.pt` | 真实数据上训练的 MEM5 权重 |
| `card_transaction.v1.csv` (2.35GB) / `transactions.tgz` (278MB) | 原始数据，不用可删 |
