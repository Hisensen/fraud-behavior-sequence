# 操作手册 — 真实数据接入五步走

> 面向：拿到北部湾（或任何）真实业务数据后的操作者。
> 全程只用一个入口 `fraud_pipeline.py`，每步一条命令，上一步的输出提示下一步。
> 环境：`pip install torch pandas scikit-learn numpy openpyxl`，CPU 即可。

---

## 事前：你需要什么样的数据

- 一张**流水/日志表**（csv / xlsx / parquet 都行），列名随意，但至少要有：
  **账户标识、精确时间戳、事件/交易类型** 三列；金额、结果、渠道、IP 有更好；
- 规模建议：**1000+ 用户**，每用户 **≥10 个事件**；
- 标签：**几十个确认的欺诈账户**就够（只用于阅卷评估，不参与训练）。
  完全没有标签也能跑，但只能输出风险排序，算不出 AUC。

---

## 第 ① 步 inspect — 看看数据长什么样

```bash
python fraud_pipeline.py inspect 北部湾流水.csv
```

输出：每列的类型/非空率/唯一值/示例 + **自动猜测的字段角色**，
并生成 `mapping.json` 模板。

**你要做的唯一人工动作**：打开 `mapping.json` 核对——
账户列猜对了吗？时间列对吗？没有的字段填 `null`。
`success_values` 里补上你们系统表示"成功"的写法（如 `"0000"`、`"S"`）。

## 第 ② 步 convert — 转成标准格式

```bash
python fraud_pipeline.py convert 北部湾流水.csv -m mapping.json -o data.jsonl
```

自动完成：按账户聚合 → 按时间排序 → 组装事件序列 → 每用户一行 jsonl。
时间解析失败的行会警告并丢弃；不足 10 个事件的用户自动过滤。

## 第 ③ 步 profile — 摸底（先别急着训模型）

```bash
python fraud_pipeline.py profile data.jsonl
```

输出两部分，**先看懂再往下走**：

1. **质量自检**：黑白样本的词频/间隔/金额/时段对比。重点检查：
   - 序列长度是否合理（太短说明聚合有问题）
   - 间隔分布是否有多样性（全挤一个桶 → 时间戳可能有问题）
   - 黑白词频差异（差异极大 → 有简单规则可用，先上规则）
2. **oracle 信号摸底**（需要标签）：五层信号各自的 AUC。读法：
   - 哪层高，信号就在哪层，模型该抓的就是它
   - **全部 ≈0.5 → 数据无信号，别浪费时间训模型，回头查数据**
   - 最高层的 AUC ≈ 数据上限；无监督最终能到"上限 − 0.03~0.07"

## 第 ④ 步 run — 训练 + 评估

```bash
python fraud_pipeline.py run data.jsonl --shape operation -o outputs/
```

`--shape` 按数据类型三选一（依据见 ARCH_REPORT.md）：

| 你的数据是 | 选 | 内部配置 |
|---|---|---|
| App 操作日志（登录/改密/转账混合） | `operation` | 自回归+时间偏置, top-k |
| 纯转账流水 | `funds` | 整事件遮罩, mean |
| 卡消费流水 | `consumption` | 字段级遮罩, top-k |

产出在 `outputs/`：
- `report.txt` — 所有打分口径的 AUC/KS/R@FPR1% + 最优口径 + 阈值
- `scores.csv` — 每个测试用户：分数、是否超阈值、**top3 可疑事件位置**（给审核员）
- `model.pt` — 模型+词表+阈值，一个文件带走

可调：`--epochs 25`（默认，数据大可减小）。

## 第 ⑤ 步 score — 日常给新数据打分

```bash
python fraud_pipeline.py score 新一批.jsonl --model outputs/model.pt -o 新分数.csv
```

输出每个账户的风险分和是否超过训练期阈值。新数据同样先经 ①② 转成 jsonl。

---

## 常见问题

**Q: 没有一个黑标签怎么办？**
①-④ 照跑（profile 会跳过 oracle，run 只输出分数和白样本 p99 阈值）。
拿分数 top 榜单给人工审核，审核结果攒起来就是未来的评估标签。

**Q: 数据里有多种业务（转账+消费+登录混在一起）？**
最好分表分别走流程（各选各的 shape），分数在账户层融合；
混在一起跑 `operation` 也能工作，只是不一定最优。

**Q: 阈值多久更新？**
建议每月用最近的白样本重算 p99（run 会打印方法）；模型每季度重训。

**Q: 效果预期？**
先看 profile 的 oracle 摸底：无监督结果 ≈ oracle 最高层 − 0.03~0.07。
oracle 都不行的数据，任何模型都救不了——问题在数据，回头查信号。

**Q: 敏感数据注意**
真实业务数据**不要提交进这个公开仓库**。本仓库脚本本地运行，数据不外传。
