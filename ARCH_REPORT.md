# 网络架构探索报告 —— 8 变体 + 3 融合 + 2 次真实数据裁决

> 日期：2026-07-03
> 目标：编码定稿(E3 全量)后自由探索架构，追求最优效果。
> 基准：data_rich(富字段合成)，V0 基线 = 0.9262；真实数据裁决用
> Sparkov(消费流)与 Ethereum(资金流)。

---

## 一、rich 基准上的变体总表（同数据/切分/打分网格）

| 变体 | AUC | R@FPR1% | 判定 |
|---|---|---|---|
| V0 基线（2层 d64, 学习式PE, 遮罩15%） | 0.9262 | 26.2% | 参照 |
| V1 加大（3层 d128 ff256, 434K参数） | 0.9250 | 23.8% | ✗ 无增益，小数据撑不起 |
| V2 时间偏置注意力（Δt分桶→每头偏置） | 0.9245 | **47.2%** | AUC平、低误报召回×1.8 |
| V3 去位置编码 | 0.9246 | 24.0% | 平——PE 冗余可省 |
| V4 遮罩率 30% | 0.9263 | 27.0% | 平 |
| V5 自回归 AR（因果，预测下一事件） | 0.9740 | 23.8% | **AUC +0.048** |
| V6 三种子集成（V0） | 0.9265 | 24.8% | 平——单模型已稳 |
| **V8 = AR + 时间偏置** | **0.9835** | **64.4%** | **双指标通吃** |
| F1/F2/F3 双通道融合 | ≤0.9805 | ≤68.8% | 不及 V8 单模型 |

**V8 机制**：AR 的严格历史条件化让"攻击链起点"（连续失败登录突现）成为最大
惊讶点——完形填空能偷看后文，语境泄漏会钝化惊讶；时间偏置让注意力显式感知
"多近算密集"。两者正交叠加：0.926 → 0.974 → 0.9835，KS 0.900，
且 AR 打分单趟完成（推理成本 = 遮罩法的 1/7）。

## 二、真实数据裁决——架构也跟数据形状走

| 数据 | 形状 | 遮罩式 MEM | AR(+偏置) | 裁决 |
|---|---|---|---|---|
| rich（合成，含因果攻击链） | 操作流 | 0.926 | **0.9835** | AR+偏置大胜 |
| Ethereum（真实钓鱼） | 资金流 | **0.9035** | 0.9023 | 打平 |
| Sparkov（真实感卡交易） | 消费流 | **0.886**（字段遮罩） | 0.786 | AR 惨败 |

解释：
- **操作流**有因果链（失败登录→换设备→改密→转出），下一步可预测 → AR 是利器；
- **资金流**（以太坊钓鱼）的异常是账户整体风格（金额模式），不是链条起点 →
  两种架构等价，瓶颈不在架构；
- **消费流**下一笔本质随机，AR 的预测目标天然高熵 → 信号被淹没，
  字段一致性（字段遮罩）才是对的打开方式。

## 三、按数据形状的定稿架构

| 数据形状 | 定稿配置 | 依据 |
|---|---|---|
| **操作流**（App日志/盗号场景） | **AR + 时间偏置**，2层 d64，无PE，多头(type/gap/res/pamt) | rich 0.9835 |
| **资金流**（转账流水） | 事件遮罩 MEM，mean 池化，金额头为主 | eth 0.904（AR 无增益，选实现更简的） |
| **消费流**（卡交易） | 字段级遮罩 MEM + pamt，top-k | Sparkov 0.886 |

通用结论：加大模型 ✗、多种子集成 ✗、遮罩率 ✗、绝对位置编码可删（时间字段已含顺序）。

## 四、诚实边界

1. **V8 的 +0.06 只在合成基准上验证过**——因为公开领域不存在真实的银行 App
   操作日志数据。它对操作流的优势论证依赖 rich 基准的攻击链设计与真实攻击
   相似这一假设（失败登录→设备变更→转出，这是盗号的标准剧本，假设合理但未经真实数据确认）；
2. **在两份真实数据上，架构改动的收益 ≈ 0**——对比编码改动（+0.04 AUC、
   定位 +0.19、低误报召回 ×1.7，见 ENCODING_REPORT.md），本轮的定量教训是
   **字段的价值 >> 架构的价值**。预算有限时先堆字段，再调架构；
3. V8 的时间偏置计算 O(L²) 逐对时间差，序列 >500 时需要优化（分块/稀疏）。

## 五、文件索引

| 文件 | 内容 |
|---|---|
| `mem_arch.py` | 架构变体框架（MEMArch: PE/时间偏置/因果开关）+ V0~V4 |
| `mem_arch2.py` / `mem_arch3.py` | V5~V8 + 融合 |
| `sparkov_v4.py` | AR 在消费流上的裁决（负结果） |
| `eth_v2.py` | AR+偏置在真实资金流上的裁决（平局） |
