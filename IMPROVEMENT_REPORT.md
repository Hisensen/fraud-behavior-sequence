# 提升轮报告 — 三个方向五项实验的裁决

> 日期：2026-07-06
> 起因：成绩单上 TabFormer(0.687) / BankSim(0.736) / AML(0.656) 偏弱，
> 按"预期收益÷成本"执行第一梯队三项改进。

## 总对照表

| 改进项 | 之前 | 之后 | 判定 |
|---|---|---|---|
| ① TabFormer 补字段遮罩+pamt 配方 | 0.687 | **0.752**（oracle=0.760） | ✅ +6.5pp，**打平数据天花板** |
| ② BankSim 自清洗训练 | 0.686* | 0.669 | ❌ 负结果 |
| ③a AML +对手方新旧字段(cnov) | 0.656 | **0.712**（超 oracle 0.695） | ✅ +5.6pp |
| ③b Ethereum +cnov 进模型 | 0.9035 | 0.9041 | ≈平（机制不匹配，见下） |
| ③c Ethereum cnov 分数级融合 | 0.9035 | **0.9281**，R@FPR1% 14.7%→**44.9%** | ✅ +2.5pp，低误报召回×3 |

\* 同口径复现值；v1 发布的 0.736 为另一打分组合。

## 各项细节

### ① TabFormer（tabformer_v3.py）
把 Sparkov 的"字段级遮罩 + 个人相对金额桶"配方补给同为消费流的 TabFormer。
窗口级 0.687→0.752，已到 oracle(0.760) 的误差范围内——**这份数据的序列信号
被榨干了**。代价：逐笔定位从 0.833 降到 0.761（字段遮罩打分对定位不如整事件
遮罩+chip）。**定稿形态：双通道**——v3 管窗口判黑，v2 管笔级定位。

### ② BankSim 自清洗（banksim_v2.py）——负结果
思路：先粗训→给训练集自打分→踢掉最高 18%→重训。实际：清洗命中率仅 32%
（相对 19% 基础污染率 lift 1.7×），污染 19%→16%，同时误删难样本，AUC 反降
(0.686→0.669)，仅定位微升(0.761→0.781)。**死结在于：基础模型本身被污染拖累，
它给出的清洗信号就不准。** 结论：污染 >15% 的数据，自清洗救不了，
正解是拿更干净的训练源或用少量标签做污染过滤。

### ③ 对手方新旧字段（cnov）——一条重要的机制规律
cnov = 该笔交易的对手方在此账户历史中已出现的次数（首次/2-3次/4次+），
在抽取时计算，不需要建图——"用序列字段偷渡图信号"。

- **AML：成立**。裸特征 0.614，进模型后 type+cnov 组合 0.712，
  超过旧 oracle(0.695)——这是标签模型都没有的信息维度；
- **Ethereum：进模型≈无效(0.9041)，但裸特征高达 0.817**。原因是机制不匹配：
  "首次对手方占比"是**账户级静态比例**，不是"惊讶"模式——模型学会
  "这种语境对手方就是全新的"之后预测得很准，CE 反而低，
  信号不会从 masked-prediction 通道冒出来；
- **正确用法：分数级融合**。MEM 异常分(0.9035) 与裸特征(0.8168) 秩融合
  (权重 0.6/0.4) → **0.9281**，KS 0.718，R@FPR1% 14.7%→41.6~44.9%，
  距 oracle(0.934) 仅 0.006。

**新规律入册：判别信号分两类——"模式类"（序列/字段的组合异常）进
masked/AR 预测通道；"聚合类"（比例、计数、静态画像）进分数融合通道。
往预测通道里塞聚合特征是无效的。**

## 更新后的完整成绩单

| 数据集 | 最初 | 现在 | oracle | 状态 |
|---|---|---|---|---|
| rich 合成（操作流） | 0.926 | **0.984** | ~0.99 | 榨干 |
| temporal 合成 | 0.32(反向) | **0.959** | 0.999 | 榨干 |
| Ethereum 钓鱼(真实) | 0.904 | **0.928** | 0.934 | 榨干 |
| Sparkov | 0.557 | **0.886** | 0.880 | 超天花板 |
| TabFormer | 0.687 | **0.752** | 0.760 | 榨干 |
| AML | 0.656 | **0.712** | 0.695 | 超天花板 |
| BankSim | 0.736 | 0.736 | 0.891 | 卡在污染，需外部干预 |
| PaySim | 0.561 | —(不再投入) | 0.610 | 数据无信号 |

六个可提升的数据集里五个已达到或超过各自 oracle 天花板。

## 对真实数据的三条新增指引

1. 消费流形状默认双通道：字段遮罩(判黑) + 整事件遮罩(定位)；
2. **对手方新旧是必采字段**（银行数据：收款账号是否首次出现/历史出现次数），
   以裸特征+分数融合方式使用，不进预测通道；
3. 若训练数据欺诈密度可能 >15%，提前准备少量标签做污染过滤，别指望自清洗。

## 文件

`tabformer_v3.py` / `banksim_v2.py` / `eth_extract2.py` / `eth_v3.py` / `aml_v2.py`
（融合实验代码在本报告 ③c 对应的会话脚本中，已并入 pipeline 建议）
