# 编码增强消融报告 —— "编码太简单"的定量回答

> 日期：2026-07-03
> 起因：现行 MEM 编码只有 [type, 全局金额桶, 间隔桶, 时刻] 四路输入。
> 本轮把编码升级为统一 Schema（+result/channel/ip_change +个人相对金额桶），
> 在新造的富字段基准上做四档消融，并在真实卡数据(Sparkov)复验关键增量。

---

## 一、富字段基准数据 `data_rich.jsonl`

2000 用户（1500 白 + 500 黑·盗号），每个白用户有**个人画像**（金额基线 s_u
跨数量级、偏好渠道、活跃时段）。攻击信号分四层埋进不同字段，且刻意堵死捷径：

- **result 层**：攻击开始 2-3 次登录失败后成功（白样本登录失败率仅 3% 且单次）
- **channel/ip 层**：攻击切到非偏好渠道 + ip_change=1
- **个人金额层**：攻击转出 = 8~18 × 该用户基线——对穷人是天文数字但落在
  全局分布内部（富人正常转账同量级），**全局分桶看不清，只有相对个人基线能看清**
- **时序层**：攻击 ~20 分钟密集；但 **25% 白样本有合法"换手机重置"爆发**
  （登录成功+ip变化→设备变更→改密码→绑卡，同样密集），堵死纯时序捷径
- 噪声：10% 白样本有单笔 6-10× 大额消费；10% 黑样本为隐蔽型

Oracle 摸底证实各层强度（5折CV，用标签）：

| 信号层 | AUC | 说明 |
|---|---|---|
| O1 词频 | 0.549 | ≈随机 ✓ 无词频捷径 |
| O2 全局金额统计 | 0.670 | 全局分桶只能看到部分 |
| O3 个人比值统计 | 0.858 | 个人基线层 |
| O4 result/渠道/ip 统计 | 0.987 | 最强层 |
| O5 时序密集统计 | 0.796 | 合法爆发使其不再是万能钥匙 |

## 二、编码消融（同一 MEM 目标，只换输入字段）

| 档 | 字段 | AUC | R@FPR1% | 最优打分头 |
|---|---|---|---|---|
| E0 现行简单编码 | type+全局金额+gap+hour | 0.683 | 4.0% | type CE |
| E1 +result/channel/ip | E0+3字段 | **0.906** (+0.22) | 15.0% | **result CE** |
| E2 +个人相对金额桶 | E0+pamt | 0.879 (+0.20) | **46.6%** | **pamt CE** |
| E3 全量 | 全部 8 字段 | **0.912** (+0.23) | 46.0% | 全头求和 |

**结论**：
1. "编码太简单"成立且可定量——补状态/渠道字段值 **+0.22 AUC**，
   补个人基线值 **+0.20 AUC**，全量 **+0.23**；
2. 两类字段互补：E1 抬 AUC 上限（失败登录签名强），E2 抬**低误报召回**
   （个人金额偏离是高精度信号，FPR=1% 时召回 4%→46.6%，×11 倍）；
3. 每加一个字段组，最优打分头随之切换（type→res→pamt→SUM）——
   多头预测 + 分头 z-norm 的架构设计在此兑现价值。

## 三、真实数据复验（Sparkov，只加一个 pamt 字段）

| 指标 | v2（三字段） | v3（+个人相对金额桶） |
|---|---|---|
| 窗口级 AUC | 0.848 | **0.886**（单 pamt 头即最优） |
| R@FPR1% | ~29% | **48.3%** |
| 逐笔定位 AUC | 0.696 | **0.885** (+0.19) |

v3 的 0.886 已**超过**旧监督 oracle（0.880）——因为 oracle 用的是全局金额
特征，个人基线是它没有的信息维度。定位 +0.19 是全项目单笔改动的最大收益：
"这笔金额对这个人不正常"直接指认到交易粒度。

（实现细节：遮金额时全局桶与个人桶必须**同时遮**，否则互相泄漏答案。）

## 四、定稿编码方案（v2 默认配置）

统一 Schema，8 路字段，缺失用 0 号"不适用"桶：

| 字段 | 类别数 | Embedding 维 | 备注 |
|---|---|---|---|
| type 事件类型 | 按业务 | 32 | |
| result 结果 | 3 (N/A/成功/失败) | 4 | 失败签名是盗号强信号 |
| channel 渠道 | 按业务+N/A | 8 | 偏离个人偏好是信号 |
| ip_change | 3 | 4 | |
| gamt 全局金额桶 | 8 | 8 | 保留（跨用户可比） |
| **pamt 个人相对金额桶** | 8 (比值 0.25/0.5/1/2/4/8) | 16 | **本轮最大增益来源** |
| gap 间隔桶 | 9 | 16 | |
| hour 时刻 | sin/cos | 8 | 连续 |

预测头：type + gap 恒有；res / pamt 字段激活时加头。打分：分头 z-norm →
top-k / mean 双通道。个人基线生产上用滚动历史中位数（实验用序列自身中位数）。

## 五、文件索引

| 文件 | 用途 |
|---|---|
| `gen_rich_data.py` | 富字段基准生成（含合法换机爆发、个人画像） |
| `mem_rich.py` | 统一 Schema 模型 + E0~E3 消融驱动 |
| `sparkov_v3.py` | 真实数据 pamt 增量复验 |
