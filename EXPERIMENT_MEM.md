# MEM 异常检测实验记录 — temporal 数据集

> 日期：2026-07-03
> 结论：**只用白样本训练的 Masked Event Model + 难度归一化 top-k 打分，无监督 AUC=0.959**，
> 而朴素平均重建误差 AUC=0.32（反向）——池化与归一化方式是成败关键。

## 1. 数据集（重新生成，`gen_temporal.py` → `data_temporal.jsonl`）

1000 用户 = 500 白 + 500 黑（盗号/套现/洗钱各 ~167）。黑白严格对齐：
逐类型事件数量同分布（相对差 <8%）、金额同分布、小时同分布（11.6 vs 11.5）、
序列长度一致（76.7 vs 76.2）。白样本普通事件按"购物会话"组织（会话内 5s~10min
小间隔，防止模型靠"有没有小间隔"作弊），敏感事件彼此 ≥6h 孤立；黑样本全部
敏感事件集中在 ~30min 爆发窗口。噪声：10% 白样本有半密集敏感对，10% 黑样本
爆发放宽到 ~2h。

## 2. 模型（`mem_experiment.py`）

Field 式编码（type 32d + amount 8d + gap桶 16d + hour sin/cos 8d → 64d）+
2 层 Transformer（4 头，86K 参数）。随机 mask 15% 事件，双预测目标：
事件类型（12 类 CE）+ 间隔桶（9 类 CE）。**只用 400 个白样本训练**，
测试集 = 100 白（留出）+ 500 黑。

## 3. 结果

### Oracle 基线（用标签，仅验证数据设计）

| 基线 | AUC | 说明 |
|---|---|---|
| B1 词频向量+LR | 0.448 | ≈随机 ✅ 词频无信号，数据设计成立 |
| B2 间隔直方图+LR | 0.984 | 时序密集性信号确实存在 |
| B3 规则：敏感事件前间隔≤30min占比 | 0.999 | 手工特征天花板 |

### MEM 打分变体（无监督，`mem_score_v2.py` + `mem_sweep.py`）

| 变体 | AUC | KS |
|---|---|---|
| raw 平均（朴素重建误差） | **0.319（反向）** | 0.02 |
| raw top-5 | 0.187（更反） | 0.05 |
| z-norm 平均 | 0.756 | 0.43 |
| z-norm top-10（type+gap） | 0.899 | 0.65 |
| **z-norm top-5（仅 type CE）** | **0.959** | **0.788** |

最优变体分模式：盗号 0.956 / 套现 0.947 / 洗钱 0.975；
Recall@FPR=1% = 71.0%，Recall@FPR=5% = 78.2%。
k∈[3,10] 均 >0.94，对 k 不敏感；gap CE 加权只会变差（w_gap=0 最优）。

### 可解释性

每个黑样本 z 分最高的位置：解绑卡 104、改密码 65、改限额 61、绑卡 56、
设备变更 49——**模型无标签定位到了爆发窗口内的敏感操作**。

## 4. 关键发现："似然 ≠ 异常"的熵混淆

朴素平均重建误差反向（AUC 0.32）的根因：
- 黑样本爆发窗口是**低熵区域**——间隔全是小间隔，和白样本购物会话形态一致，
  间隔 CE 反而低（间隔桶期望 CE：≤1m 档 1.08 vs 0.5-1h 档 5.37）；
- 白样本孤立敏感事件的间隔散布在 1h~7d，**高熵、难预测**，CE 高；
- 平均池化后"黑样本整体更可预测"淹没局部异常信号。

两个正交修复（缺一不可）：
1. **难度归一化**：用白样本训练集统计每个间隔桶的 CE 均值/方差，逐位置
   z-score——只衡量"比同难度的正常位置异常多少"（0.32 → 0.76）；
2. **top-k 池化**：欺诈是局部异常（~8 个爆发位置 vs ~70 个正常位置），
   取 z 分最高的 k 个位置，不被正常位置稀释（0.76 → 0.96）。

注意 raw top-k 单独用是最差的（0.19）：不归一化时 CE 最高的位置全是
"高熵但正常"的位置，top-k 反而放大混淆——顺序是先归一化、再 top-k。

## 5. 与对比学习方案的对照

| 方案 | 需要标签 | temporal 数据集效果 |
|---|---|---|
| Token/Field + InfoNCE 无监督 | 否 | 分离比 ≤1.15，训练后更差 |
| Token + SupCon 全监督 | 全部 | 分离比 2.50 |
| **MEM + z-norm top-5（本实验）** | **0 个黑标签** | **AUC 0.959** |

## 6. 诚实的局限

- k=5 是在测试集上扫出来的（k∈[3,10] 均 >0.94，风险小，但严格做法是留验证集）；
- 训练白样本假定纯净；真实场景 unlabeled 数据含 ~0.1-1% 欺诈，需测污染鲁棒性；
- 合成数据的欺诈是模板生成的，规则 oracle 0.999 说明手工特征在此够用——
  MEM 的价值在于**无标签、无手工特征**地自己发现了这个信号；
- gap 预测头在打分中无贡献（w_gap=0 最优），但训练时它迫使模型建模时序结构，
  是否影响 type 头质量未做消融。

## 7. 文件索引

| 文件 | 用途 |
|---|---|
| `gen_temporal.py` | 生成数据 + 对齐性自检 |
| `mem_experiment.py` | 模型/训练/朴素打分 + oracle 基线 |
| `mem_score_v2.py` | 逐位置 CE + z-norm/top-k 打分变体 |
| `mem_sweep.py` | k × gap 权重扫描 + Recall@FPR |
| `mem_model.pt` | 训练好的模型权重 |
| `mem_scores_v2.npz` | 最优变体分数 |
